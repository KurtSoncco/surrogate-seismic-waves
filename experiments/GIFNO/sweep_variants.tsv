# GIFNO hyperparameter sweep variants (TSV: name <TAB> KEY=VAL;KEY=VAL)
# Keys map to GIFNO_<KEY> env vars (see config.apply_env_overrides).
# Empty overrides column = baseline (current config.py defaults).
baseline
h1_strong	LOSS_H1_WEIGHT=0.25
fno_wide	FNO_MODES=48,48
latent_wide	LATENT_CHANNELS=96
freq_loss	LOSS_FREQ_WEIGHT=0.05
no_mining	HARD_MINING=false
